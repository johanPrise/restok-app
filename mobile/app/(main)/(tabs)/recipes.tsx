import { ComingSoon } from '@/components/ComingSoon';

export default function Recipes() {
  return (
    <ComingSoon
      title="Recettes"
      description="Des recettes construites à partir de ce que le groupe a réellement en stock."
    />
  );
}
